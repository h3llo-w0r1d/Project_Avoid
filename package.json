{
  "name": "avoidarc",
  "version": "1.0.0",
  "description": "전기선 피하기 서바이벌 게임",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env server.js",
    "dev": "node --env-file-if-exists=.env --watch server.js",
    "vendor": "node scripts/vendor-three.js"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.21.2",
    "maxmind": "^4.3.29",
    "ws": "^8.21.2"
  },
  "devDependencies": {
    "three": "^0.169.0"
  }
}
