# 작업 방식

## 코드는 ponytail 로

코드를 쓰거나 고치거나 검토하거나 라이브러리를 고를 때는 항상 ponytail 스킬을
먼저 적용한다. 원칙은 셋이다.

- **먼저 이 일이 필요한지 묻는다.** 안 만드는 게 제일 짧다.
- **표준 라이브러리 → 플랫폼 기능 → 의존성** 순으로 본다. 의존성은 마지막이다.
- **한 줄로 되면 쉰 줄을 쓰지 않는다.** 구현체가 하나뿐인 추상화, 호출자가
  하나뿐인 계층, 아무도 안 바꾸는 설정은 만들지 않는다.

일부러 짧게 가느라 남긴 빚은 `ponytail:` 주석으로 표시한다. `/ponytail-debt` 로 모아 본다.

## 이 저장소에서 지킬 것

- 주석·커밋 메시지·UI 문구는 **한국어**로 쓴다.
- 주석은 "무엇을" 이 아니라 **"왜"** 를 적는다. 기존 주석의 밀도와 말투를 따른다.
- Node 22 이상 전제다. `node:sqlite` 를 쓰므로 SQLite 라이브러리를 따로 넣지 않는다.
- `public/js/shared/` 는 서버와 브라우저가 같이 돌린다. three.js 를 import 하지 않는다.
- `public/js/vendor/` 는 복사본이다. 직접 고치지 말고 `npm run vendor` 로 다시 뜬다.
